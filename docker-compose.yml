services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        dll: "docke_web_Api.dll"
    image: docke_web_api:offline
    container_name: docke_web_api
    environment:
      ASPNETCORE_URLS: "http://0.0.0.0:8080"
      ASPNETCORE_ENVIRONMENT: "LOC"  # "Production"
    expose:
      - "8080"
    networks:
      - edge
    logging: 
        driver: json-file
        options:
            max-size: "10m"
            max-file: "5"


  nginx:
    image: nginx:alpine
    container_name: nginx
    depends_on:
      - api    # simplified, no health condition
    ports:
      - "8085:8085" # Map host port 8085 to container port 8085
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - edge
    logging: 
        driver: json-file
        options:
            max-size: "10m"
            max-file: "5"

networks:
  edge:
    driver: bridge