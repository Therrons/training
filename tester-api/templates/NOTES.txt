Thank you for installing {{ .Chart.Name }}!

Namespace:  {{ .Values.namespace.name }}
Release:    {{ .Release.Name }}
Service:    {{ include "tester-api.fullname" . }} (ClusterIP on port {{ .Values.service.port }})

Ingress:
  Host:     {{ .Values.ingress.host }}
  Class:    {{ .Values.ingress.className }}
  URL:      http://{{ .Values.ingress.host }}/swagger

Troubleshooting:
  kubectl get all -n {{ .Values.namespace.name }}
  kubectl get ingress -n {{ .Values.namespace.name }}
  kubectl logs -f -n {{ .Values.namespace.name }} deploy/{{ include "tester-api.fullname" . }}
  kubectl port-forward -n {{ .Values.namespace.name }} svc/{{ include "tester-api.fullname" . }} 8085:{{ .Values.service.port }}