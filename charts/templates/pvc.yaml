{{- if and .Values.persistence.enabled (not .Values.persistence.perReplica) (not .Values.persistence.existingClaim) -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "fraud-poc-api.fullname" . }}-pvc
  namespace: {{ .Values.namespace.name | default "fraud-poc-api" }}
  labels:
    {{- include "fraud-poc-api.labels" . | nindent 4 }}
spec:
  accessModes: {{ toYaml .Values.persistence.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  {{- if .Values.persistence.storageClassName }}
  storageClassName: {{ .Values.persistence.storageClassName }}
  {{- end }}
{{- end }}
